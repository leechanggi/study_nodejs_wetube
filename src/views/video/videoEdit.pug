extends ../layout.pug 

block content
  h4 Change Title of #{video.title}
  form(method="POST" action=`/videos/${video.id}/edit`)
    input(type="text" name="title" placeholder="Video Title" value=pageTitle required)
    input(type="submit" value="Save")